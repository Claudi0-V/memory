const firstPage = "let's start the game";
const secondPage = "it will mix";
const thirdPage = "lets see what happens";
const FirstGameContent = [firstPage, secondPage, thirdPage];

export default FirstGameContent;
